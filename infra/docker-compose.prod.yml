services:
  # ═══════════════════════════════════════════════════
  # INFRAESTRUCTURA COMPARTIDA
  # ═══════════════════════════════════════════════════
  rabbitmq:
    image: rabbitmq:3-management
    container_name: nachopps-rabbitmq
    restart: unless-stopped
    # Sin puertos expuestos al host
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-nachopps}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}
    healthcheck:
      test: [ 'CMD', 'rabbitmq-diagnostics', 'check_running' ]
      interval: 10s
      timeout: 5s
      retries: 5

  # BASES DE DATOS (Solo red interna)
  db-reservas:
    image: postgres:16-alpine
    container_name: nachopps-db-reservas
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: reservas_db
    volumes:
      - nachopps-db-reservas:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d reservas_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  db-mesas:
    image: postgres:16-alpine
    container_name: nachopps-db-mesas
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: mesas_db
    volumes:
      - nachopps-db-mesas:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d mesas_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  db-pedidos:
    image: postgres:16-alpine
    container_name: nachopps-db-pedidos
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: pedidos_db
    volumes:
      - nachopps-db-pedidos:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d pedidos_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  db-cuentas:
    image: postgres:16-alpine
    container_name: nachopps-db-cuentas
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: cuentas_db
    volumes:
      - nachopps-db-cuentas:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d cuentas_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  db-inventario:
    image: postgres:16-alpine
    container_name: nachopps-db-inventario
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: inventario_db
    volumes:
      - nachopps-db-inventario:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d inventario_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  db-reportes:
    image: postgres:16-alpine
    container_name: nachopps-db-reportes
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: reportes_db
    volumes:
      - nachopps-db-reportes:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d reportes_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  db-facturacion:
    image: postgres:16-alpine
    container_name: nachopps-db-facturacion
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: facturacion_db
    volumes:
      - nachopps-db-facturacion:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d facturacion_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  db-compras:
    image: postgres:16-alpine
    container_name: nachopps-db-compras
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: compras_db
    volumes:
      - nachopps-db-compras:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d compras_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  db-caja:
    image: postgres:16-alpine
    container_name: nachopps-db-caja
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: caja_db
    volumes:
      - nachopps-db-caja:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d caja_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  db-identidad:
    image: postgres:16-alpine
    container_name: nachopps-db-identidad
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: identidad_db
    volumes:
      - nachopps-db-identidad:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d identidad_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  db-notificaciones:
    image: postgres:16-alpine
    container_name: nachopps-db-notificaciones
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-nachopps}
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      POSTGRES_DB: notificaciones_db
    volumes:
      - nachopps-db-notificaciones:/var/lib/postgresql/data
    healthcheck:
      test: [ 'CMD-SHELL', 'pg_isready -U ${DB_USER:-nachopps} -d notificaciones_db' ]
      interval: 5s
      timeout: 3s
      retries: 5

  # ═══════════════════════════════════════════════════
  # MICROSERVICIOS (Solo red interna)
  # ═══════════════════════════════════════════════════
  servicio-identidad:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-identidad:latest
    container_name: nachopps-servicio-identidad
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-identidad:5432/identidad_db?schema=public'
      SERVICE_NAME: 'servicio-identidad'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-15m}
      REFRESH_TOKEN_TTL_DAYS: ${REFRESH_TOKEN_TTL_DAYS:-7}
      JWT_PRIVATE_KEY: ${JWT_PRIVATE_KEY:?JWT_PRIVATE_KEY es obligatorio en identidad (PEM RS256)}
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-identidad:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-mesas:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-mesas:latest
    container_name: nachopps-servicio-mesas
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-mesas:5432/mesas_db?schema=public'
      SERVICE_NAME: 'servicio-mesas'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-mesas:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-pedidos:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-pedidos:latest
    container_name: nachopps-servicio-pedidos
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-pedidos:5432/pedidos_db?schema=public'
      SERVICE_NAME: 'servicio-pedidos'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      INVENTARIO_SERVICE_URL: 'http://servicio-inventario:3000/api'
      MESAS_SERVICE_URL: 'http://servicio-mesas:3000/api'
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-pedidos:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-cuentas:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-cuentas:latest
    container_name: nachopps-servicio-cuentas
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-cuentas:5432/cuentas_db?schema=public'
      SERVICE_NAME: 'servicio-cuentas'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      PEDIDOS_SERVICE_URL: 'http://servicio-pedidos:3000/api'
      MESAS_SERVICE_URL: 'http://servicio-mesas:3000/api'
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-cuentas:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-reservas:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-reservas:latest
    container_name: nachopps-servicio-reservas
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-reservas:5432/reservas_db?schema=public'
      SERVICE_NAME: 'servicio-reservas'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-reservas:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-inventario:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-inventario:latest
    container_name: nachopps-servicio-inventario
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-inventario:5432/inventario_db?schema=public'
      SERVICE_NAME: 'servicio-inventario'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-inventario:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-notificaciones:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-notificaciones:latest
    container_name: nachopps-servicio-notificaciones
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-notificaciones:5432/notificaciones_db?schema=public'
      SERVICE_NAME: 'servicio-notificaciones'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-notificaciones:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-caja:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-caja:latest
    container_name: nachopps-servicio-caja
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-caja:5432/caja_db?schema=public'
      SERVICE_NAME: 'servicio-caja'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      CUENTAS_SERVICE_URL: 'http://servicio-cuentas:3000/api'
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-caja:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-reportes:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-reportes:latest
    container_name: nachopps-servicio-reportes
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-reportes:5432/reportes_db?schema=public'
      SERVICE_NAME: 'servicio-reportes'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-reportes:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-facturacion:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-facturacion:latest
    container_name: nachopps-servicio-facturacion
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-facturacion:5432/facturacion_db?schema=public'
      SERVICE_NAME: 'servicio-facturacion'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
      # SUNAT: opcionales a propósito (${VAR:-}, no ${VAR:?}) — el deploy no
      # debe fallar si aún no tienes certificado/SOL de una o ambas empresas.
      # Mientras falten, CertificadoService/EnvioProcessor lo detectan y ese
      # comprobante queda FIRMADO sin enviar (nunca se pierde, no gasta intentos).
      # Fallback de producción — coincide con .env.example. Si algún día se
      # necesita volver a homologación, sobreescribe SUNAT_WSDL_URL en .env
      # con la URL de e-beta.sunat.gob.pe (no toques este default).
      SUNAT_WSDL_URL: ${SUNAT_WSDL_URL:-https://e-factura.sunat.gob.pe/ol-ti-itcpfegem/billService?wsdl}
      SUNAT_PFX_EMPRESA_1_FILE: ${SUNAT_PFX_EMPRESA_1_FILE:-}
      SUNAT_PFX_EMPRESA_1_PASS: ${SUNAT_PFX_EMPRESA_1_PASS:-}
      SUNAT_SOL_USER_EMPRESA_1: ${SUNAT_SOL_USER_EMPRESA_1:-}
      SUNAT_SOL_PASS_EMPRESA_1: ${SUNAT_SOL_PASS_EMPRESA_1:-}
      SUNAT_PFX_EMPRESA_2_FILE: ${SUNAT_PFX_EMPRESA_2_FILE:-}
      SUNAT_PFX_EMPRESA_2_PASS: ${SUNAT_PFX_EMPRESA_2_PASS:-}
      SUNAT_SOL_USER_EMPRESA_2: ${SUNAT_SOL_USER_EMPRESA_2:-}
      SUNAT_SOL_PASS_EMPRESA_2: ${SUNAT_SOL_PASS_EMPRESA_2:-}
      # Cifra las credenciales SUNAT cargadas por el formulario "Configurar
      # SUNAT" de Facturación — sin esto el formulario no puede guardar (pero
      # el servicio sigue andando con las 8 variables SUNAT_*_EMPRESA_<n>_*
      # de arriba). No bloquea el deploy si aún no se generó (${:-}).
      FACTURACION_CRED_ENCRYPTION_KEY: ${FACTURACION_CRED_ENCRYPTION_KEY:-}
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-facturacion:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-compras:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/servicio-compras:latest
    container_name: nachopps-servicio-compras
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    stop_grace_period: 30s
    environment:
      PORT: '3000'
      DATABASE_URL: 'postgresql://${DB_USER:-nachopps}:${DB_PASS:?DB_PASS es obligatorio en produccion}@db-compras:5432/compras_db?schema=public'
      SERVICE_NAME: 'servicio-compras'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://${RABBITMQ_USER:-nachopps}:${RABBITMQ_PASS:?RABBITMQ_PASS es obligatorio en produccion}@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY:?JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      SERVICE_JWT_SECRET: ${SERVICE_JWT_SECRET:?SERVICE_JWT_SECRET es obligatorio (tokens S2S internos)}
      NODE_ENV: 'production'
      CORS_ORIGIN: ${CORS_ORIGIN:?CORS_ORIGIN es obligatorio, p.ej. https://app.tudominio.com}
      COMPROBANTES_DIR: '/data/comprobantes'
      COMPROBANTE_MAX_BYTES: ${COMPROBANTE_MAX_BYTES:-12582912}
      COMPROBANTE_MAX_LADO: ${COMPROBANTE_MAX_LADO:-1600}
      COMPROBANTE_WEBP_QUALITY: ${COMPROBANTE_WEBP_QUALITY:-72}
    volumes:
      # Fotos de boletas/facturas de compra: deben entrar en la rutina de
      # backup junto a nachopps-backups (ver bloque de backups más abajo).
      - nachopps-comprobantes:/data/comprobantes
    healthcheck:
      test: [ 'CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live' ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db-compras:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # ═══════════════════════════════════════════════════
  # FRONTEND (PWA)
  # ═══════════════════════════════════════════════════
  pwa-cliente:
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/pwa-cliente:latest
    container_name: nachopps-pwa-cliente
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    ports:
      - '8080:8080'

  # ═══════════════════════════════════════════════════
  # KONG API GATEWAY (Punto único de acceso)
  # ═══════════════════════════════════════════════════
  kong:
    # Se construye y publica en GHCR vía .github/workflows/deploy.yml (igual
    # que los demás servicios) — sin `build:` acá para que Watchtower pueda
    # actualizarlo igual que a todos: comparando el digest de la imagen
    # publicada, no reconstruyéndolo localmente cada vez.
    image: ${REGISTRY:-ghcr.io/Maczz09/mi-narcita}/kong:latest
    container_name: nachopps-kong
    labels:
      - com.centurylinklabs.watchtower.enable=true
    restart: unless-stopped
    environment:
      KONG_DATABASE: 'off'
      # No exponer versión de Kong/nginx en cabeceras (hallazgo ZAP 10036)
      KONG_HEADERS: 'off'
      KONG_DECLARATIVE_CONFIG: /kong/kong.yml
      KONG_PROXY_ACCESS_LOG: /dev/stdout
      KONG_ADMIN_ACCESS_LOG: /dev/stdout
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_ERROR_LOG: /dev/stderr
      # Admin API solo en loopback DENTRO del contenedor: inalcanzable desde la
      # red de Docker y desde el host. Para administrar: `docker exec` o túnel SSH.
      KONG_ADMIN_LISTEN: 127.0.0.1:8001
      KONG_PROXY_LISTEN: 0.0.0.0:8000
      KONG_PLUGINS: 'bundled,jwt-cache'
      KONG_JWT_PUBLIC_KEY: ${KONG_JWT_PUBLIC_KEY:?KONG_JWT_PUBLIC_KEY es obligatorio (PEM RS256, \n literales)}
      KONG_RATE_LIMIT_MINUTE: ${KONG_RATE_LIMIT_MINUTE:-3000}
      KONG_RATE_LIMIT_HOUR: ${KONG_RATE_LIMIT_HOUR:-30000}
      # Dominio(s) https reales de la PWA. Obligatorio en producción.
      KONG_CORS_ORIGINS: ${KONG_CORS_ORIGINS:?KONG_CORS_ORIGINS es obligatorio, p.ej. ["https://app.tudominio.com"]}
      KONG_NGINX_HTTP_LUA_SHARED_DICT: 'jwt_cache 12m'
    ports:
      - '8000:8000'
      # La Admin API (8001) NO se publica en prod: queda en loopback interno.
    volumes:
      - ./kong/kong.yml.template:/tmp/kong.yml.template:ro
    healthcheck:
      test: [ 'CMD', 'kong', 'health' ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # ═══════════════════════════════════════════════════
  # OBSERVABILIDAD
  # ═══════════════════════════════════════════════════
  # OTel Collector real (contrib): recibe OTLP de los servicios y exporta a
  # Jaeger. Los servicios apuntan a http://otel-collector:4318 (sin cambios).
  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    container_name: nachopps-otel-collector
    command: [ '--config=/etc/otel-collector/otel-collector-config.yml' ]
    volumes:
      - ./otel-collector/otel-collector-config.yml:/etc/otel-collector/otel-collector-config.yml:ro
    depends_on:
      - jaeger
    restart: unless-stopped
    # Sin puertos al host: los servicios lo alcanzan por la red interna.

    # La imagen :latest corre como UID 10001 no-root; un volumen nombrado nuevo
    # se crea con dueño root, así que sin este init jaeger truena en el primer
    # deploy con "mkdir /badger/key: permission denied" (visto en el despliegue
    # real a la VPS del 2026-08-14). El compose de dev usa `user: root` como
    # atajo — en prod se evita correr el propio Jaeger como root, y en cambio
    # se corrige el dueño del volumen una sola vez antes de arrancarlo.
  jaeger-init:
    image: alpine:3
    container_name: nachopps-jaeger-init
    volumes:
      - jaeger_data:/badger
    command: [ 'chown', '-R', '10001:10001', '/badger' ]
    restart: 'no'

  # Backend de trazas persistente (Badger en volumen). UI 16686 solo por red
  # interna / túnel SSH (sin publicar al host en prod).
  jaeger:
    image: jaegertracing/all-in-one:latest
    container_name: nachopps-jaeger
    depends_on:
      jaeger-init:
        condition: service_completed_successfully
    environment:
      SPAN_STORAGE_TYPE: badger
      BADGER_EPHEMERAL: 'false'
      BADGER_DIRECTORY_VALUE: /badger/data
      BADGER_DIRECTORY_KEY: /badger/key
    volumes:
      - jaeger_data:/badger
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    container_name: nachopps-prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--web.enable-lifecycle'
      # Persistencia de exemplars (trace_id) para el salto métrica→Jaeger.
      - '--enable-feature=exemplar-storage'
    restart: unless-stopped
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus/alert.rules.yml:/etc/prometheus/alert.rules.yml:ro
      - prometheus_data:/prometheus

  alertmanager:
    image: prom/alertmanager:latest
    container_name: nachopps-alertmanager
    # Instancia única, sin peers de cluster configurados: sin esto el gossip
    # mesh puede fallar a arrancar ("no private IP found") en algunos hosts.
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
      - '--cluster.listen-address='
    restart: unless-stopped
    volumes:
      - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro

  grafana:
    image: grafana/grafana:latest
    container_name: nachopps-grafana
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASS:?GRAFANA_PASS es obligatorio en produccion}
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana_data:/var/lib/grafana

  # Backups de las 9 BDs (plan 3.5): pg_dump diario + retención. Para almacenar
  # fuera del host, montar un bind a un volumen de red/objeto o sincronizar
  # nachopps-backups a S3 con un job aparte.
  db-backup:
    image: postgres:16-alpine
    container_name: nachopps-db-backup
    environment:
      DB_USER: ${DB_USER:-nachopps}
      DB_PASS: ${DB_PASS:?DB_PASS es obligatorio en produccion}
      RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-7}
      BACKUP_DIR: /backups
    volumes:
      - ../scripts/backup-postgres.sh:/usr/local/bin/backup.sh:ro
      - nachopps-backups:/backups
    entrypoint: [ '/bin/sh', '-c', 'while true; do sh /usr/local/bin/backup.sh || echo "[db-backup] ciclo con fallos"; sleep 86400; done' ]
    restart: unless-stopped

  watchtower:
    image: containrrr/watchtower:latest
    container_name: nachopps-watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ~/.docker/config.json:/config.json:ro
    environment:
      - DOCKER_API_VERSION=1.40
    # --label-enable: solo toca contenedores con el label
    # com.centurylinklabs.watchtower.enable=true (los 13 que publica
    # .github/workflows/deploy.yml). Sin esto, Watchtower actualiza CUALQUIER
    # contenedor del host — incluidas las 11 bases Postgres, RabbitMQ,
    # Grafana, etc. — apenas Docker Hub publique un nuevo build de esas
    # imágenes, reiniciándolas sin aviso. Nada de eso lo construye este repo;
    # no debe auto-actualizarse solo.
    command: --interval 300 --cleanup --label-enable
    restart: unless-stopped

volumes:
  nachopps-backups: # Observabilidad persistente.

  prometheus_data:
  grafana_data:
  jaeger_data:
  nachopps-db-reservas:
  nachopps-db-mesas:
  nachopps-db-pedidos:
  nachopps-db-cuentas:
  nachopps-db-inventario:
  nachopps-db-caja:
  nachopps-db-reportes:
  nachopps-db-facturacion:
  nachopps-db-compras: # Fotos de boletas/facturas de compra (WebP + miniatura). Debe entrar en la

  # rutina de backup junto a nachopps-backups.
  nachopps-comprobantes:
  nachopps-db-identidad:
  nachopps-db-notificaciones:
