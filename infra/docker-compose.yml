x-jwt-dev-env: &jwt_dev_env ./secrets/jwt-dev.env

# SOLO DESARROLLO. Credenciales debiles a proposito (postgres `secret`,
# admin `nachopps123`, `SERVICE_JWT_SECRET` fijo). NUNCA usar este archivo en
# un VPS/host publico; produccion usa docker-compose.prod.yml + .env real.
services:
  # ═══════════════════════════════════════════════════
  # INFRAESTRUCTURA COMPARTIDA
  # ═══════════════════════════════════════════════════
  rabbitmq:
    image: rabbitmq:3-management
    container_name: nachopps-rabbitmq
    # A-4: sin mem_limit el memory watermark del broker (40 % de la RAM) depende
    # del host → backpressure irreproducible en pruebas. 1g fija el umbral en ~400MB.
    mem_limit: 1g
    restart: unless-stopped # la VM de Docker Desktop murió bajo carga: el broker debe autolevantar
    ports:
      # A-4/host: 5672/15672 chocan con otro stack Docker de este host
      # (medicitas_rabbitmq); remapeados solo del lado host. La comunicación
      # interna entre servicios sigue usando `rabbitmq:5672` (red de Docker),
      # no se ve afectada.
      - '5674:5672'
      - '15674:15672'
      - '15692:15692' # A-2: métricas Prometheus del broker
    environment:
      RABBITMQ_DEFAULT_USER: nachopps
      RABBITMQ_DEFAULT_PASS: nachopps_secret
    volumes:
      # A-2: habilita rabbitmq_prometheus (profundidad de colas/DLQ visible en Grafana)
      - ./rabbitmq/enabled_plugins:/etc/rabbitmq/enabled_plugins:ro
    profiles: ['infra', 'all', 'dev']
    healthcheck:
      test: ['CMD', 'rabbitmq-diagnostics', 'check_running']
      interval: 10s
      timeout: 5s
      retries: 5

  # BASES DE DATOS
  db-reservas:
    image: postgres:16-alpine
    container_name: nachopps-db-reservas
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: reservas_db
    ports: ['5441:5432']
    profiles: ['infra', 'all', 'dev']
    # Hallazgo de carga (2026-07-13): sin restart, una caída por presión de
    # memoria del host deja al servicio dueño en loop de "Database not ready"
    # indefinido — el resto del stack sí se autorecupera, la BD no lo hacía.
    # mem_limit contiene el fallo a UN contenedor en vez de arrastrar a los 9.
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-reservas:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d reservas_db']
      interval: 5s
      timeout: 3s
      retries: 5

  db-mesas:
    image: postgres:16-alpine
    container_name: nachopps-db-mesas
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: mesas_db
    ports: ['5433:5432']
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-mesas:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d mesas_db']
      interval: 5s
      timeout: 3s
      retries: 5

  db-pedidos:
    image: postgres:16-alpine
    container_name: nachopps-db-pedidos
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: pedidos_db
    ports: ['5434:5432']
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-pedidos:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d pedidos_db']
      interval: 5s
      timeout: 3s
      retries: 5

  db-cuentas:
    image: postgres:16-alpine
    container_name: nachopps-db-cuentas
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: cuentas_db
    ports: ['5435:5432']
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-cuentas:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d cuentas_db']
      interval: 5s
      timeout: 3s
      retries: 5

  db-facturacion:
    image: postgres:16-alpine
    container_name: nachopps-db-facturacion
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: facturacion_db
    ports: ['5442:5432']
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-facturacion:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d facturacion_db']
      interval: 5s
      timeout: 3s
      retries: 5

  db-compras:
    image: postgres:16-alpine
    container_name: nachopps-db-compras
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: compras_db
    ports: ['5443:5432']
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-compras:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d compras_db']
      interval: 5s
      timeout: 3s
      retries: 5

  db-inventario:
    image: postgres:16-alpine
    container_name: nachopps-db-inventario
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: inventario_db
    ports: ['5436:5432']
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-inventario:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d inventario_db']
      interval: 5s
      timeout: 3s
      retries: 5

  db-reportes:
    image: postgres:16-alpine
    container_name: nachopps-db-reportes
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: reportes_db
    ports: ['5438:5432']
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-reportes:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d reportes_db']
      interval: 5s
      timeout: 3s
      retries: 5

  db-caja:
    image: postgres:16-alpine
    container_name: nachopps-db-caja
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: caja_db
    ports: ['5437:5432']
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-caja:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d caja_db']
      interval: 5s
      timeout: 3s
      retries: 5

  db-identidad:
    image: postgres:16-alpine
    container_name: nachopps-db-identidad
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: identidad_db
    ports: ['5439:5432']
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-identidad:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d identidad_db']
      interval: 5s
      timeout: 3s
      retries: 5

  db-notificaciones:
    image: postgres:16-alpine
    container_name: nachopps-db-notificaciones
    environment:
      POSTGRES_USER: nachopps
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: notificaciones_db
    ports: ['5440:5432']
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-db-notificaciones:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nachopps -d notificaciones_db']
      interval: 5s
      timeout: 3s
      retries: 5

  # ═══════════════════════════════════════════════════
  # MICROSERVICIOS
  # ═══════════════════════════════════════════════════
  servicio-identidad:
    image: mi-narcita-servicio-identidad
    container_name: nachopps-servicio-identidad
    # host 3001 choca con otro stack Docker de este host (medicitas_grafana);
    # Kong sigue enrutando internamente a servicio-identidad:3000, sin cambios.
    ports: ['3021:3000']
    env_file: *jwt_dev_env
    environment:
      PORT: '3000'
      NODE_OPTIONS: '--max-old-space-size=400'
      # Hallazgo de carga (2026-07-13): prefetch 20 > pool pg de 10 → bajo
      # tormenta de eventos (pedidos L1) los consumidores no consiguen iniciar
      # transacción (P2028), agotan los 3 reintentos y mandan miles de mensajes
      # a la DLQ. Con 5, los mensajes esperan en el broker (backpressure
      # durable) en vez de morir: en vuelo ≤ 5 conexiones, quedan 5 para HTTP.
      RMQ_PREFETCH: '5'
      # Hallazgo de carga (2026-07-13): con pool=10, 1000 escrituras concurrentes
      # → P2028 (transacción no consigue conexión) → ~10k respuestas 500 en L1.
      # 25 por servicio deja margen amplio frente a max_connections=100 de su
      # Postgres dedicado (1 servicio por BD + crons de outbox/purga).
      PG_POOL_MAX: '40'
      # Medido en L1 final: con maxWait 10s quedaba un tail de 5.4% de P2028 en
      # pedidos (~150 escritores concurrentes). 30s convierte ese tail en
      # respuestas lentas (backpressure) en vez de 500; 40 conexiones suben el
      # ritmo de servicio de tx (Postgres dedicado: max_connections=100).
      PG_TX_MAXWAIT_MS: '30000'
      # Medido: el connectionTimeoutMillis del pg.Pool (5s, B-1) salta ANTES que
      # el maxWait de Prisma cuando los GETs concurrentes retienen las 40
      # conexiones (sort de listados sin índice) → P2028 en escrituras. En
      # sesión de carga: esperar = backpressure; 5s solo tiene sentido con
      # tráfico normal.
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      # B-5: 200 ev/s de drenado por productor (el default de 50 dejaba backlogs
      # de >10 min tras ráfagas de escritura de los niveles L2/L3).
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-identidad:5432/identidad_db?schema=public'
      SERVICE_NAME: 'servicio-identidad'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
      # Parametrizable: la suite k6 (stress-tests/k6) sube esto en la sesión de
      # pruebas (JWT_EXPIRES_IN=60m) para que el token no caduque a mitad de un
      # run L2/L3 largo. Default sin cambios para uso normal.
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-15m}
      REFRESH_TOKEN_TTL_DAYS: '7'
      COOKIE_SECURE: 'false'
      COOKIE_SAMESITE: 'strict'
    profiles: ['all']
    restart: unless-stopped # A-4: paridad con prod; `docker compose stop` manual sigue funcionando
    # Hallazgo de carga (2026-07-13): sin mem_limit, un contenedor que crece
    # bajo saturación puede agotar el presupuesto compartido de la VM de Docker
    # Desktop (~7.7GB) y arrastrar a los 9 Postgres consigo (visto en runtime:
    # los 9 db-* murieron a la vez, exit 255, sin OOM de cgroup individual).
    # 512m×9 servicios: la suma debe caber en la VM de Docker Desktop (~7.7GB);
    # con 768m la VM entera murió dos veces bajo L1 de pedidos (fan-out de
    # escrituras). NODE_OPTIONS fuerza a V8 a hacer GC antes del cap del cgroup
    # (V8 no siempre respeta límites de contenedor) → presión de GC en vez de
    # OOM-kill.
    mem_limit: 512m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-identidad:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-mesas:
    image: mi-narcita-servicio-mesas
    container_name: nachopps-servicio-mesas
    ports: ['3002:3000']
    env_file: *jwt_dev_env
    environment:
      PORT: '3000'
      NODE_OPTIONS: '--max-old-space-size=400'
      # Hallazgo de carga (2026-07-13): prefetch 20 > pool pg de 10 → bajo
      # tormenta de eventos (pedidos L1) los consumidores no consiguen iniciar
      # transacción (P2028), agotan los 3 reintentos y mandan miles de mensajes
      # a la DLQ. Con 5, los mensajes esperan en el broker (backpressure
      # durable) en vez de morir: en vuelo ≤ 5 conexiones, quedan 5 para HTTP.
      RMQ_PREFETCH: '5'
      # Hallazgo de carga (2026-07-13): con pool=10, 1000 escrituras concurrentes
      # → P2028 (transacción no consigue conexión) → ~10k respuestas 500 en L1.
      # 25 por servicio deja margen amplio frente a max_connections=100 de su
      # Postgres dedicado (1 servicio por BD + crons de outbox/purga).
      PG_POOL_MAX: '40'
      # Medido en L1 final: con maxWait 10s quedaba un tail de 5.4% de P2028 en
      # pedidos (~150 escritores concurrentes). 30s convierte ese tail en
      # respuestas lentas (backpressure) en vez de 500; 40 conexiones suben el
      # ritmo de servicio de tx (Postgres dedicado: max_connections=100).
      PG_TX_MAXWAIT_MS: '30000'
      # Medido: el connectionTimeoutMillis del pg.Pool (5s, B-1) salta ANTES que
      # el maxWait de Prisma cuando los GETs concurrentes retienen las 40
      # conexiones (sort de listados sin índice) → P2028 en escrituras. En
      # sesión de carga: esperar = backpressure; 5s solo tiene sentido con
      # tráfico normal.
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      # B-5: 200 ev/s de drenado por productor (el default de 50 dejaba backlogs
      # de >10 min tras ráfagas de escritura de los niveles L2/L3).
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-mesas:5432/mesas_db?schema=public'
      SERVICE_NAME: 'servicio-mesas'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
    profiles: ['all']
    restart: unless-stopped # A-4: paridad con prod; `docker compose stop` manual sigue funcionando
    # Hallazgo de carga (2026-07-13): sin mem_limit, un contenedor que crece
    # bajo saturación puede agotar el presupuesto compartido de la VM de Docker
    # Desktop (~7.7GB) y arrastrar a los 9 Postgres consigo (visto en runtime:
    # los 9 db-* murieron a la vez, exit 255, sin OOM de cgroup individual).
    # 512m×9 servicios: la suma debe caber en la VM de Docker Desktop (~7.7GB);
    # con 768m la VM entera murió dos veces bajo L1 de pedidos (fan-out de
    # escrituras). NODE_OPTIONS fuerza a V8 a hacer GC antes del cap del cgroup
    # (V8 no siempre respeta límites de contenedor) → presión de GC en vez de
    # OOM-kill.
    mem_limit: 512m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-mesas:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-pedidos:
    image: mi-narcita-servicio-pedidos
    container_name: nachopps-servicio-pedidos
    ports: ['3004:3000']
    env_file: *jwt_dev_env
    environment:
      PORT: '3000'
      NODE_OPTIONS: '--max-old-space-size=400'
      # Hallazgo de carga (2026-07-13): prefetch 20 > pool pg de 10 → bajo
      # tormenta de eventos (pedidos L1) los consumidores no consiguen iniciar
      # transacción (P2028), agotan los 3 reintentos y mandan miles de mensajes
      # a la DLQ. Con 5, los mensajes esperan en el broker (backpressure
      # durable) en vez de morir: en vuelo ≤ 5 conexiones, quedan 5 para HTTP.
      RMQ_PREFETCH: '5'
      # Hallazgo de carga (2026-07-13): con pool=10, 1000 escrituras concurrentes
      # → P2028 (transacción no consigue conexión) → ~10k respuestas 500 en L1.
      # 25 por servicio deja margen amplio frente a max_connections=100 de su
      # Postgres dedicado (1 servicio por BD + crons de outbox/purga).
      PG_POOL_MAX: '40'
      # Medido en L1 final: con maxWait 10s quedaba un tail de 5.4% de P2028 en
      # pedidos (~150 escritores concurrentes). 30s convierte ese tail en
      # respuestas lentas (backpressure) en vez de 500; 40 conexiones suben el
      # ritmo de servicio de tx (Postgres dedicado: max_connections=100).
      PG_TX_MAXWAIT_MS: '30000'
      # Medido: el connectionTimeoutMillis del pg.Pool (5s, B-1) salta ANTES que
      # el maxWait de Prisma cuando los GETs concurrentes retienen las 40
      # conexiones (sort de listados sin índice) → P2028 en escrituras. En
      # sesión de carga: esperar = backpressure; 5s solo tiene sentido con
      # tráfico normal.
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      # B-5: 200 ev/s de drenado por productor (el default de 50 dejaba backlogs
      # de >10 min tras ráfagas de escritura de los niveles L2/L3).
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-pedidos:5432/pedidos_db?schema=public'
      SERVICE_NAME: 'servicio-pedidos'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      INVENTARIO_SERVICE_URL: 'http://servicio-inventario:3000/api'
      MESAS_SERVICE_URL: 'http://servicio-mesas:3000/api'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
    profiles: ['all']
    restart: unless-stopped # A-4: paridad con prod; `docker compose stop` manual sigue funcionando
    # Hallazgo de carga (2026-07-13): sin mem_limit, un contenedor que crece
    # bajo saturación puede agotar el presupuesto compartido de la VM de Docker
    # Desktop (~7.7GB) y arrastrar a los 9 Postgres consigo (visto en runtime:
    # los 9 db-* murieron a la vez, exit 255, sin OOM de cgroup individual).
    # 512m×9 servicios: la suma debe caber en la VM de Docker Desktop (~7.7GB);
    # con 768m la VM entera murió dos veces bajo L1 de pedidos (fan-out de
    # escrituras). NODE_OPTIONS fuerza a V8 a hacer GC antes del cap del cgroup
    # (V8 no siempre respeta límites de contenedor) → presión de GC en vez de
    # OOM-kill.
    mem_limit: 512m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-pedidos:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-cuentas:
    image: mi-narcita-servicio-cuentas
    container_name: nachopps-servicio-cuentas
    ports: ['3005:3000']
    env_file: *jwt_dev_env
    environment:
      PORT: '3000'
      NODE_OPTIONS: '--max-old-space-size=400'
      # Hallazgo de carga (2026-07-13): prefetch 20 > pool pg de 10 → bajo
      # tormenta de eventos (pedidos L1) los consumidores no consiguen iniciar
      # transacción (P2028), agotan los 3 reintentos y mandan miles de mensajes
      # a la DLQ. Con 5, los mensajes esperan en el broker (backpressure
      # durable) en vez de morir: en vuelo ≤ 5 conexiones, quedan 5 para HTTP.
      RMQ_PREFETCH: '5'
      # Hallazgo de carga (2026-07-13): con pool=10, 1000 escrituras concurrentes
      # → P2028 (transacción no consigue conexión) → ~10k respuestas 500 en L1.
      # 25 por servicio deja margen amplio frente a max_connections=100 de su
      # Postgres dedicado (1 servicio por BD + crons de outbox/purga).
      PG_POOL_MAX: '40'
      # Medido en L1 final: con maxWait 10s quedaba un tail de 5.4% de P2028 en
      # pedidos (~150 escritores concurrentes). 30s convierte ese tail en
      # respuestas lentas (backpressure) en vez de 500; 40 conexiones suben el
      # ritmo de servicio de tx (Postgres dedicado: max_connections=100).
      PG_TX_MAXWAIT_MS: '30000'
      # Medido: el connectionTimeoutMillis del pg.Pool (5s, B-1) salta ANTES que
      # el maxWait de Prisma cuando los GETs concurrentes retienen las 40
      # conexiones (sort de listados sin índice) → P2028 en escrituras. En
      # sesión de carga: esperar = backpressure; 5s solo tiene sentido con
      # tráfico normal.
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      # B-5: 200 ev/s de drenado por productor (el default de 50 dejaba backlogs
      # de >10 min tras ráfagas de escritura de los niveles L2/L3).
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-cuentas:5432/cuentas_db?schema=public'
      SERVICE_NAME: 'servicio-cuentas'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      PEDIDOS_SERVICE_URL: 'http://servicio-pedidos:3000/api'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
    profiles: ['all']
    restart: unless-stopped # A-4: paridad con prod; `docker compose stop` manual sigue funcionando
    # Hallazgo de carga (2026-07-13): sin mem_limit, un contenedor que crece
    # bajo saturación puede agotar el presupuesto compartido de la VM de Docker
    # Desktop (~7.7GB) y arrastrar a los 9 Postgres consigo (visto en runtime:
    # los 9 db-* murieron a la vez, exit 255, sin OOM de cgroup individual).
    # 512m×9 servicios: la suma debe caber en la VM de Docker Desktop (~7.7GB);
    # con 768m la VM entera murió dos veces bajo L1 de pedidos (fan-out de
    # escrituras). NODE_OPTIONS fuerza a V8 a hacer GC antes del cap del cgroup
    # (V8 no siempre respeta límites de contenedor) → presión de GC en vez de
    # OOM-kill.
    mem_limit: 512m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-cuentas:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-reservas:
    image: mi-narcita-servicio-reservas
    container_name: nachopps-servicio-reservas
    ports: ['3006:3000']
    env_file: *jwt_dev_env
    environment:
      PORT: '3000'
      NODE_OPTIONS: '--max-old-space-size=400'
      # Hallazgo de carga (2026-07-13): prefetch 20 > pool pg de 10 → bajo
      # tormenta de eventos (pedidos L1) los consumidores no consiguen iniciar
      # transacción (P2028), agotan los 3 reintentos y mandan miles de mensajes
      # a la DLQ. Con 5, los mensajes esperan en el broker (backpressure
      # durable) en vez de morir: en vuelo ≤ 5 conexiones, quedan 5 para HTTP.
      RMQ_PREFETCH: '5'
      # Hallazgo de carga (2026-07-13): con pool=10, 1000 escrituras concurrentes
      # → P2028 (transacción no consigue conexión) → ~10k respuestas 500 en L1.
      # 25 por servicio deja margen amplio frente a max_connections=100 de su
      # Postgres dedicado (1 servicio por BD + crons de outbox/purga).
      PG_POOL_MAX: '40'
      # Medido en L1 final: con maxWait 10s quedaba un tail de 5.4% de P2028 en
      # pedidos (~150 escritores concurrentes). 30s convierte ese tail en
      # respuestas lentas (backpressure) en vez de 500; 40 conexiones suben el
      # ritmo de servicio de tx (Postgres dedicado: max_connections=100).
      PG_TX_MAXWAIT_MS: '30000'
      # Medido: el connectionTimeoutMillis del pg.Pool (5s, B-1) salta ANTES que
      # el maxWait de Prisma cuando los GETs concurrentes retienen las 40
      # conexiones (sort de listados sin índice) → P2028 en escrituras. En
      # sesión de carga: esperar = backpressure; 5s solo tiene sentido con
      # tráfico normal.
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      # B-5: 200 ev/s de drenado por productor (el default de 50 dejaba backlogs
      # de >10 min tras ráfagas de escritura de los niveles L2/L3).
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-reservas:5432/reservas_db?schema=public'
      SERVICE_NAME: 'servicio-reservas'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
    profiles: ['all']
    restart: unless-stopped # A-4: paridad con prod; `docker compose stop` manual sigue funcionando
    # Hallazgo de carga (2026-07-13): sin mem_limit, un contenedor que crece
    # bajo saturación puede agotar el presupuesto compartido de la VM de Docker
    # Desktop (~7.7GB) y arrastrar a los 9 Postgres consigo (visto en runtime:
    # los 9 db-* murieron a la vez, exit 255, sin OOM de cgroup individual).
    # 512m×9 servicios: la suma debe caber en la VM de Docker Desktop (~7.7GB);
    # con 768m la VM entera murió dos veces bajo L1 de pedidos (fan-out de
    # escrituras). NODE_OPTIONS fuerza a V8 a hacer GC antes del cap del cgroup
    # (V8 no siempre respeta límites de contenedor) → presión de GC en vez de
    # OOM-kill.
    mem_limit: 512m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-reservas:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-inventario:
    image: mi-narcita-servicio-inventario
    container_name: nachopps-servicio-inventario
    ports: ['3007:3000']
    env_file: *jwt_dev_env
    environment:
      PORT: '3000'
      NODE_OPTIONS: '--max-old-space-size=400'
      # Hallazgo de carga (2026-07-13): prefetch 20 > pool pg de 10 → bajo
      # tormenta de eventos (pedidos L1) los consumidores no consiguen iniciar
      # transacción (P2028), agotan los 3 reintentos y mandan miles de mensajes
      # a la DLQ. Con 5, los mensajes esperan en el broker (backpressure
      # durable) en vez de morir: en vuelo ≤ 5 conexiones, quedan 5 para HTTP.
      RMQ_PREFETCH: '5'
      # Hallazgo de carga (2026-07-13): con pool=10, 1000 escrituras concurrentes
      # → P2028 (transacción no consigue conexión) → ~10k respuestas 500 en L1.
      # 25 por servicio deja margen amplio frente a max_connections=100 de su
      # Postgres dedicado (1 servicio por BD + crons de outbox/purga).
      PG_POOL_MAX: '40'
      # Medido en L1 final: con maxWait 10s quedaba un tail de 5.4% de P2028 en
      # pedidos (~150 escritores concurrentes). 30s convierte ese tail en
      # respuestas lentas (backpressure) en vez de 500; 40 conexiones suben el
      # ritmo de servicio de tx (Postgres dedicado: max_connections=100).
      PG_TX_MAXWAIT_MS: '30000'
      # Medido: el connectionTimeoutMillis del pg.Pool (5s, B-1) salta ANTES que
      # el maxWait de Prisma cuando los GETs concurrentes retienen las 40
      # conexiones (sort de listados sin índice) → P2028 en escrituras. En
      # sesión de carga: esperar = backpressure; 5s solo tiene sentido con
      # tráfico normal.
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      # B-5: 200 ev/s de drenado por productor (el default de 50 dejaba backlogs
      # de >10 min tras ráfagas de escritura de los niveles L2/L3).
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-inventario:5432/inventario_db?schema=public'
      SERVICE_NAME: 'servicio-inventario'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
    profiles: ['all']
    restart: unless-stopped # A-4: paridad con prod; `docker compose stop` manual sigue funcionando
    # Hallazgo de carga (2026-07-13): sin mem_limit, un contenedor que crece
    # bajo saturación puede agotar el presupuesto compartido de la VM de Docker
    # Desktop (~7.7GB) y arrastrar a los 9 Postgres consigo (visto en runtime:
    # los 9 db-* murieron a la vez, exit 255, sin OOM de cgroup individual).
    # 512m×9 servicios: la suma debe caber en la VM de Docker Desktop (~7.7GB);
    # con 768m la VM entera murió dos veces bajo L1 de pedidos (fan-out de
    # escrituras). NODE_OPTIONS fuerza a V8 a hacer GC antes del cap del cgroup
    # (V8 no siempre respeta límites de contenedor) → presión de GC en vez de
    # OOM-kill.
    mem_limit: 512m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-inventario:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-notificaciones:
    image: mi-narcita-servicio-notificaciones
    container_name: nachopps-servicio-notificaciones
    ports: ['3008:3000']
    env_file: *jwt_dev_env
    environment:
      PORT: '3000'
      NODE_OPTIONS: '--max-old-space-size=400'
      # Hallazgo de carga (2026-07-13): prefetch 20 > pool pg de 10 → bajo
      # tormenta de eventos (pedidos L1) los consumidores no consiguen iniciar
      # transacción (P2028), agotan los 3 reintentos y mandan miles de mensajes
      # a la DLQ. Con 5, los mensajes esperan en el broker (backpressure
      # durable) en vez de morir: en vuelo ≤ 5 conexiones, quedan 5 para HTTP.
      RMQ_PREFETCH: '5'
      # Hallazgo de carga (2026-07-13): con pool=10, 1000 escrituras concurrentes
      # → P2028 (transacción no consigue conexión) → ~10k respuestas 500 en L1.
      # 25 por servicio deja margen amplio frente a max_connections=100 de su
      # Postgres dedicado (1 servicio por BD + crons de outbox/purga).
      PG_POOL_MAX: '40'
      # Medido en L1 final: con maxWait 10s quedaba un tail de 5.4% de P2028 en
      # pedidos (~150 escritores concurrentes). 30s convierte ese tail en
      # respuestas lentas (backpressure) en vez de 500; 40 conexiones suben el
      # ritmo de servicio de tx (Postgres dedicado: max_connections=100).
      PG_TX_MAXWAIT_MS: '30000'
      # Medido: el connectionTimeoutMillis del pg.Pool (5s, B-1) salta ANTES que
      # el maxWait de Prisma cuando los GETs concurrentes retienen las 40
      # conexiones (sort de listados sin índice) → P2028 en escrituras. En
      # sesión de carga: esperar = backpressure; 5s solo tiene sentido con
      # tráfico normal.
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      # B-5: 200 ev/s de drenado por productor (el default de 50 dejaba backlogs
      # de >10 min tras ráfagas de escritura de los niveles L2/L3).
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-notificaciones:5432/notificaciones_db?schema=public'
      SERVICE_NAME: 'servicio-notificaciones'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
    profiles: ['all']
    restart: unless-stopped # A-4: paridad con prod; `docker compose stop` manual sigue funcionando
    # Hallazgo de carga (2026-07-13): sin mem_limit, un contenedor que crece
    # bajo saturación puede agotar el presupuesto compartido de la VM de Docker
    # Desktop (~7.7GB) y arrastrar a los 9 Postgres consigo (visto en runtime:
    # los 9 db-* murieron a la vez, exit 255, sin OOM de cgroup individual).
    # 512m×9 servicios: la suma debe caber en la VM de Docker Desktop (~7.7GB);
    # con 768m la VM entera murió dos veces bajo L1 de pedidos (fan-out de
    # escrituras). NODE_OPTIONS fuerza a V8 a hacer GC antes del cap del cgroup
    # (V8 no siempre respeta límites de contenedor) → presión de GC en vez de
    # OOM-kill.
    mem_limit: 512m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-notificaciones:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-caja:
    image: mi-narcita-servicio-caja
    container_name: nachopps-servicio-caja
    ports: ['3009:3000']
    env_file: *jwt_dev_env
    environment:
      PORT: '3000'
      NODE_OPTIONS: '--max-old-space-size=400'
      # Hallazgo de carga (2026-07-13): prefetch 20 > pool pg de 10 → bajo
      # tormenta de eventos (pedidos L1) los consumidores no consiguen iniciar
      # transacción (P2028), agotan los 3 reintentos y mandan miles de mensajes
      # a la DLQ. Con 5, los mensajes esperan en el broker (backpressure
      # durable) en vez de morir: en vuelo ≤ 5 conexiones, quedan 5 para HTTP.
      RMQ_PREFETCH: '5'
      # Hallazgo de carga (2026-07-13): con pool=10, 1000 escrituras concurrentes
      # → P2028 (transacción no consigue conexión) → ~10k respuestas 500 en L1.
      # 25 por servicio deja margen amplio frente a max_connections=100 de su
      # Postgres dedicado (1 servicio por BD + crons de outbox/purga).
      PG_POOL_MAX: '40'
      # Medido en L1 final: con maxWait 10s quedaba un tail de 5.4% de P2028 en
      # pedidos (~150 escritores concurrentes). 30s convierte ese tail en
      # respuestas lentas (backpressure) en vez de 500; 40 conexiones suben el
      # ritmo de servicio de tx (Postgres dedicado: max_connections=100).
      PG_TX_MAXWAIT_MS: '30000'
      # Medido: el connectionTimeoutMillis del pg.Pool (5s, B-1) salta ANTES que
      # el maxWait de Prisma cuando los GETs concurrentes retienen las 40
      # conexiones (sort de listados sin índice) → P2028 en escrituras. En
      # sesión de carga: esperar = backpressure; 5s solo tiene sentido con
      # tráfico normal.
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      # B-5: 200 ev/s de drenado por productor (el default de 50 dejaba backlogs
      # de >10 min tras ráfagas de escritura de los niveles L2/L3).
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-caja:5432/caja_db?schema=public'
      SERVICE_NAME: 'servicio-caja'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      CUENTAS_SERVICE_URL: 'http://servicio-cuentas:3000/api'
      # Micro-caché del resumen de turno (dato de dashboard): bajo consulta
      # concurrente masiva evita recomputar la agregación por request.
      CAJA_RESUMEN_TTL_MS: '5000'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
    profiles: ['all']
    restart: unless-stopped # A-4: paridad con prod; `docker compose stop` manual sigue funcionando
    # Hallazgo de carga (2026-07-13): sin mem_limit, un contenedor que crece
    # bajo saturación puede agotar el presupuesto compartido de la VM de Docker
    # Desktop (~7.7GB) y arrastrar a los 9 Postgres consigo (visto en runtime:
    # los 9 db-* murieron a la vez, exit 255, sin OOM de cgroup individual).
    # 512m×9 servicios: la suma debe caber en la VM de Docker Desktop (~7.7GB);
    # con 768m la VM entera murió dos veces bajo L1 de pedidos (fan-out de
    # escrituras). NODE_OPTIONS fuerza a V8 a hacer GC antes del cap del cgroup
    # (V8 no siempre respeta límites de contenedor) → presión de GC en vez de
    # OOM-kill.
    mem_limit: 512m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-caja:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-reportes:
    image: mi-narcita-servicio-reportes
    container_name: nachopps-servicio-reportes
    ports: ['3010:3000']
    env_file: *jwt_dev_env
    environment:
      PORT: '3000'
      NODE_OPTIONS: '--max-old-space-size=400'
      # Hallazgo de carga (2026-07-13): prefetch 20 > pool pg de 10 → bajo
      # tormenta de eventos (pedidos L1) los consumidores no consiguen iniciar
      # transacción (P2028), agotan los 3 reintentos y mandan miles de mensajes
      # a la DLQ. Con 5, los mensajes esperan en el broker (backpressure
      # durable) en vez de morir: en vuelo ≤ 5 conexiones, quedan 5 para HTTP.
      RMQ_PREFETCH: '5'
      # Hallazgo de carga (2026-07-13): con pool=10, 1000 escrituras concurrentes
      # → P2028 (transacción no consigue conexión) → ~10k respuestas 500 en L1.
      # 25 por servicio deja margen amplio frente a max_connections=100 de su
      # Postgres dedicado (1 servicio por BD + crons de outbox/purga).
      PG_POOL_MAX: '40'
      # Medido en L1 final: con maxWait 10s quedaba un tail de 5.4% de P2028 en
      # pedidos (~150 escritores concurrentes). 30s convierte ese tail en
      # respuestas lentas (backpressure) en vez de 500; 40 conexiones suben el
      # ritmo de servicio de tx (Postgres dedicado: max_connections=100).
      PG_TX_MAXWAIT_MS: '30000'
      # Medido: el connectionTimeoutMillis del pg.Pool (5s, B-1) salta ANTES que
      # el maxWait de Prisma cuando los GETs concurrentes retienen las 40
      # conexiones (sort de listados sin índice) → P2028 en escrituras. En
      # sesión de carga: esperar = backpressure; 5s solo tiene sentido con
      # tráfico normal.
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      # B-5: 200 ev/s de drenado por productor (el default de 50 dejaba backlogs
      # de >10 min tras ráfagas de escritura de los niveles L2/L3).
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-reportes:5432/reportes_db?schema=public'
      SERVICE_NAME: 'servicio-reportes'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
    profiles: ['all']
    restart: unless-stopped # A-4: paridad con prod; `docker compose stop` manual sigue funcionando
    # Hallazgo de carga (2026-07-13): sin mem_limit, un contenedor que crece
    # bajo saturación puede agotar el presupuesto compartido de la VM de Docker
    # Desktop (~7.7GB) y arrastrar a los 9 Postgres consigo (visto en runtime:
    # los 9 db-* murieron a la vez, exit 255, sin OOM de cgroup individual).
    # 512m×9 servicios: la suma debe caber en la VM de Docker Desktop (~7.7GB);
    # con 768m la VM entera murió dos veces bajo L1 de pedidos (fan-out de
    # escrituras). NODE_OPTIONS fuerza a V8 a hacer GC antes del cap del cgroup
    # (V8 no siempre respeta límites de contenedor) → presión de GC en vez de
    # OOM-kill.
    mem_limit: 512m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-reportes:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-facturacion:
    image: mi-narcita-servicio-facturacion
    container_name: nachopps-servicio-facturacion
    ports: ['3011:3000']
    # sunat-dev.env es opcional a proposito (required: false): igual que el
    # .p12 del volumen de abajo, no existe en un checkout fresco (CI, otro
    # dev) — el servicio ya esta preparado para arrancar "no configurado"
    # (ver SunatConfigService) y sin esto docker compose up fallaba entero
    # por un env_file requerido inexistente.
    env_file:
      - *jwt_dev_env
      - path: ./secrets/sunat-dev.env
        required: false
    volumes:
      - ../certificates:/app/certificates:ro
    environment:
      PORT: '3000'
      NODE_OPTIONS: '--max-old-space-size=400'
      RMQ_PREFETCH: '5'
      PG_POOL_MAX: '40'
      PG_TX_MAXWAIT_MS: '30000'
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-facturacion:5432/facturacion_db?schema=public'
      SERVICE_NAME: 'servicio-facturacion'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
      # SUNAT: credenciales de empresa 1 (Salitral 1, RUC 10417758432) desde
      # infra/secrets/sunat-dev.env (gitignorado); el .p12 se monta como
      # volumen desde certificates/ (también gitignorado, nunca en la imagen).
      # Empresa 2 queda "no configurada" hasta tramitar su propio certificado
      # — CertificadoService/EnvioProcessor lo detectan sin tumbar el resto.
      # A propósito en BETA/homologación, aunque prod ya usa el endpoint real
      # (ver .env.example) — este stack es para desarrollo local; nunca debe
      # poder enviar un comprobante real a SUNAT solo por estar probando algo.
      SUNAT_WSDL_URL: 'https://e-beta.sunat.gob.pe/ol-ti-itcpfegem-beta/billService?wsdl'
      SUNAT_PFX_EMPRESA_1_FILE: '/app/certificates/salitral-1/certificado.p12'
      # Cifra las credenciales SUNAT que se suban por el formulario "Configurar
      # SUNAT" de Facturación (ver CredencialesCryptoService) — 32 bytes hex,
      # SOLO de dev. En prod, un secret real generado con
      # `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`.
      FACTURACION_CRED_ENCRYPTION_KEY: '165525fec81a294571de597b1110d6b17e5060da219c87dcb865da257e7f0d69'
    profiles: ['all']
    restart: unless-stopped
    mem_limit: 512m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-facturacion:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  servicio-compras:
    image: mi-narcita-servicio-compras
    container_name: nachopps-servicio-compras
    ports: ['3012:3000']
    env_file: *jwt_dev_env
    environment:
      PORT: '3000'
      # 320 y no 400 como el resto: sharp/libvips asigna FUERA del heap de V8.
      # Con 400 + arena nativo el cgroup de 512m mata el contenedor al procesar
      # una foto grande.
      NODE_OPTIONS: '--max-old-space-size=320'
      RMQ_PREFETCH: '5'
      # 15 y no 40: servicio de baja concurrencia (solo ADMIN/GERENCIA/SISTEMA),
      # sin consumidores de eventos. No hace falta reservar 40 conexiones.
      PG_POOL_MAX: '15'
      PG_TX_MAXWAIT_MS: '30000'
      PG_POOL_CONN_TIMEOUT_MS: '30000'
      OUTBOX_BATCH_SIZE: '200'
      DATABASE_URL: 'postgresql://nachopps:secret@db-compras:5432/compras_db?schema=public'
      SERVICE_NAME: 'servicio-compras'
      SERVICE_AUD_ENFORCE: 'true'
      RABBITMQ_URI: 'amqp://nachopps:nachopps_secret@rabbitmq:5672'
      OTEL_EXPORTER_OTLP_ENDPOINT: 'http://otel-collector:4318'
      SERVICE_JWT_SECRET: 'nachopps_service_secret_dev'
      CORS_ORIGIN: 'http://localhost:4200'
      COMPROBANTES_DIR: '/data/comprobantes'
      COMPROBANTE_MAX_BYTES: '12582912' # 12 MB antes de comprimir
      COMPROBANTE_MAX_LADO: '1600'
      COMPROBANTE_WEBP_QUALITY: '72'
    profiles: ['all']
    restart: unless-stopped
    mem_limit: 512m
    volumes:
      - nachopps-comprobantes:/data/comprobantes
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/api/health/live']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      db-compras:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # ═══════════════════════════════════════════════════
  # KONG API GATEWAY (con jwt-cache)
  # ═══════════════════════════════════════════════════
  kong:
    build:
      context: kong
      dockerfile: Dockerfile
    image: mi-narcita-kong
    container_name: nachopps-kong
    env_file: *jwt_dev_env
    environment:
      KONG_DATABASE: 'off'
      # No exponer versión de Kong/nginx en cabeceras (hallazgo ZAP 10036)
      KONG_HEADERS: 'off'
      KONG_DECLARATIVE_CONFIG: /kong/kong.yml
      KONG_PROXY_ACCESS_LOG: /dev/stdout
      KONG_ADMIN_ACCESS_LOG: /dev/stdout
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_ERROR_LOG: /dev/stderr
      KONG_ADMIN_LISTEN: 0.0.0.0:8001
      KONG_PROXY_LISTEN: 0.0.0.0:8000
      KONG_PLUGINS: 'bundled,jwt-cache'
      KONG_RATE_LIMIT_MINUTE: ${KONG_RATE_LIMIT_MINUTE:-3000}
      KONG_RATE_LIMIT_HOUR: ${KONG_RATE_LIMIT_HOUR:-30000}
      KONG_CORS_ORIGINS: ${KONG_CORS_ORIGINS:-["http://localhost:4200","http://localhost:4201","http://localhost:5173","http://127.0.0.1:4200","http://127.0.0.1:4201","http://127.0.0.1:5173"]}
      KONG_NGINX_HTTP_LUA_SHARED_DICT: 'jwt_cache 12m'
      # Medido 2026-07-13: con `auto`, nginx levanta 1 worker/CPU (12) y el
      # conjunto no cabe en el mem_limit → el kernel mata workers (signal 9) y
      # Kong se recicla en plena carga. 4 workers sobran para este throughput.
      KONG_NGINX_WORKER_PROCESSES: '4'
      # Compras sube fotos de comprobante (request-size-limiting del servicio
      # ya limita a 16 MB; esto evita que el default de nginx de Kong corte
      # antes con un 413 sin cuerpo informativo).
      KONG_NGINX_HTTP_CLIENT_MAX_BODY_SIZE: '16m'
    ports:
      - '8000:8000'
      - '8001:8001'
    volumes:
      - ./kong/kong.yml.template:/tmp/kong.yml.template:ro
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped # la VM de Docker Desktop murió bajo carga: el gateway debe autolevantar
    # 768m (medido en L2: con 512m el gateway rozaba el 97% con 4 workers y
    # perdía workers por OOM-kill bajo meseta sostenida).
    mem_limit: 768m
    healthcheck:
      test: ['CMD', 'kong', 'health']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # ═══════════════════════════════════════════════════
  # OBSERVABILIDAD
  # ═══════════════════════════════════════════════════
  # OTel Collector real (contrib): recibe OTLP de los 9 servicios (grpc 4317 /
  # http 4318) y exporta las trazas a Jaeger. Los servicios siguen apuntando a
  # http://otel-collector:4318, así que no cambia código de aplicación.
  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    container_name: nachopps-otel-collector
    command: ['--config=/etc/otel-collector/otel-collector-config.yml']
    volumes:
      - ./otel-collector/otel-collector-config.yml:/etc/otel-collector/otel-collector-config.yml:ro
    # host 4317/4318 chocan con otro stack Docker de este host (medicitas_jaeger);
    # remapeados solo del lado host. Los servicios apuntan a
    # http://otel-collector:4318 (nombre de red interno), sin cambios.
    ports:
      - '4328:4318'
      - '4327:4317'
    depends_on:
      - jaeger
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 256m

  # Backend de trazas con almacenamiento persistente (Badger en volumen): las
  # trazas y la UI (16686) sobreviven reinicios. Antes era all-in-one en memoria.
  jaeger:
    image: jaegertracing/all-in-one:latest
    container_name: nachopps-jaeger
    # La imagen :latest pasó a correr como UID 10001 no-root; el volumen nombrado
    # se crea con dueño root y el proceso no puede mkdir /badger/key ni /badger/data.
    # ponytail: root en dev alcanza (SOLO DESARROLLO, ver cabecera del archivo); si
    # esto pasa a prod, fijar el volumen con la UID 10001 en vez de correr como root.
    user: root
    environment:
      SPAN_STORAGE_TYPE: badger
      BADGER_EPHEMERAL: 'false'
      BADGER_DIRECTORY_VALUE: /badger/data
      BADGER_DIRECTORY_KEY: /badger/key
    # host 16686 choca con otro stack Docker de este host (medicitas_jaeger).
    ports:
      - '16687:16686'
    volumes:
      - jaeger_data:/badger
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    # Badger reserva 256m solo de BlockCache + 185 tablas al abrir; con 512m
    # la DB acumulada no entraba y el kernel lo mataba por OOM (exit 137) en bucle.
    mem_limit: 1g

  prometheus:
    image: prom/prometheus:latest
    container_name: nachopps-prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--web.enable-lifecycle'
      # Almacena los exemplars scrapeados (trace_id embebido en los histogramas
      # OpenMetrics de los servicios); sin este flag Prometheus los descarta y
      # el salto métrica→traza en Grafana queda vacío.
      - '--enable-feature=exemplar-storage'
    # host 9090 choca con otro stack Docker de este host (medicitas_prometheus).
    ports:
      - '9091:9090'
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus/alert.rules.yml:/etc/prometheus/alert.rules.yml:ro
      - prometheus_data:/prometheus
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 512m

  alertmanager:
    image: prom/alertmanager:latest
    container_name: nachopps-alertmanager
    # Instancia única, sin peers de cluster configurados: en algunos hosts
    # Docker Desktop no puede deducir una IP privada para el gossip mesh
    # ("no private IP found") y el proceso muere en loop de restart.
    # --cluster.listen-address='' apaga el clustering (innecesario aquí).
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
      - '--cluster.listen-address='
    # host 9093 choca con otro stack Docker de este host (medicitas_alertmanager).
    ports:
      - '9095:9093'
    volumes:
      - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 128m

  grafana:
    image: grafana/grafana:latest
    container_name: nachopps-grafana
    ports:
      - '3000:3000'
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana_data:/var/lib/grafana
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 384m

  # Agregación de logs estructurados (plan 5.1): Loki almacena, Promtail recoge
  # el stdout JSON (con trace_id/correlationId) de los contenedores.
  loki:
    image: grafana/loki:2.9.0
    container_name: nachopps-loki
    command: -config.file=/etc/loki/loki-config.yml
    ports:
      - '3100:3100'
    volumes:
      - ./loki/loki-config.yml:/etc/loki/loki-config.yml:ro
      - loki_data:/loki
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 384m

  promtail:
    image: grafana/promtail:2.9.0
    container_name: nachopps-promtail
    command: -config.file=/etc/promtail/promtail-config.yml
    volumes:
      - ./promtail/promtail-config.yml:/etc/promtail/promtail-config.yml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      - loki
    profiles: ['infra', 'all', 'dev']
    restart: unless-stopped
    mem_limit: 128m

  pwa-cliente:
    build:
      context: ../
      dockerfile: apps/pwa-cliente/Dockerfile
    image: mi-narcita-pwa-cliente
    container_name: nachopps-pwa-cliente
    ports:
      - '4200:8080'
    profiles: ['all']
    restart: unless-stopped
    mem_limit: 128m

volumes:
  nachopps-db-reservas:
  nachopps-db-mesas:
  nachopps-db-pedidos:
  nachopps-db-cuentas:
  nachopps-db-facturacion:
  nachopps-db-compras:
  # Fotos de boletas/facturas de compra (WebP + miniatura). Persiste a
  # `docker compose down`; NO va a la BD para no arrastrar blobs por el heap.
  nachopps-comprobantes:
  nachopps-db-inventario:
  nachopps-db-caja:
  nachopps-db-reportes:
  nachopps-db-identidad:
  nachopps-db-notificaciones:
  # Observabilidad persistente (sobrevive a `docker compose down`).
  prometheus_data:
  grafana_data:
  loki_data:
  jaeger_data:
