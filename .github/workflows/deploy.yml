name: CI/CD Pipeline

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  REGISTRY_NAME: ghcr.io/${{ github.repository_owner }}/mi-narcita

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      # Sin esto, un solo servicio que falla (p. ej. un blip transitorio de
      # red/GHCR al hacer push) cancela el build-and-push de los otros 12 —
      # se pierden pushes que sí habrían salido bien, y el log solo muestra
      # "Cancelled" en vez de la causa real.
      fail-fast: false
      matrix:
        service:
          - name: servicio-identidad
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-identidad
          - name: servicio-mesas
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-mesas
          - name: servicio-pedidos
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-pedidos
          - name: servicio-cuentas
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-cuentas
          - name: servicio-reservas
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-reservas
          - name: servicio-inventario
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-inventario
          - name: servicio-notificaciones
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-notificaciones
          - name: servicio-caja
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-caja
          - name: servicio-reportes
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-reportes
          - name: servicio-facturacion
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-facturacion
          - name: servicio-compras
            dockerfile: Dockerfile
            context: .
            build-args: APP_NAME=servicio-compras
          - name: pwa-cliente
            dockerfile: apps/pwa-cliente/Dockerfile
            context: .
            # Repo Settings → Secrets and variables → Actions → Variables →
            # VITE_API_BASE_URL = https://tu-dominio-real (sin esto, cae al
            # placeholder de abajo y la PWA en prod llamaría a un dominio que
            # no es el suyo — login/CORS rotos en silencio).
            build-args: VITE_API_BASE_URL=${{ vars.VITE_API_BASE_URL || 'https://cambia-esto-en-settings-variables.example' }}
          - name: kong
            dockerfile: infra/kong/Dockerfile
            context: infra/kong
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.service.context }}
          file: ${{ matrix.service.dockerfile }}
          push: true
          build-args: ${{ matrix.service.build-args }}
          tags: ${{ env.REGISTRY_NAME }}/${{ matrix.service.name }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
